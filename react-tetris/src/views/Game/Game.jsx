import React from "react";
import { Card, CardHeader, CardBody, Divider, Image } from "@nextui-org/react";

const Game = () => {
	return <canvas width="320" height="640" id="game"></canvas>;
};

export default Game;
